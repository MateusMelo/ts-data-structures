{
  "name": "ts-data-structures",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc -w",
    "test": "npm run build && node --test dist/tests/**/*.spec.js",
    "test:watch": "npm run build && node --test --watch dist/tests/**/*.spec.js",
    "clean": "rm -rf dist"
  },
  "keywords": [],
  "author": "MateusMelo",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.8.10",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  }
}
